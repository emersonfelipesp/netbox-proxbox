# Sub-PR L Playwright TODO

`tests/playwright/` infrastructure is not present in this checkout, so the
Sub-PR L full intent lifecycle Playwright spec was skipped.

When the Playwright harness exists, add `tests/playwright/intent_full_flow.spec.ts`
covering branch creation, plan summary, apply job streaming, deletion request
approval, and executed deletion audit visibility.

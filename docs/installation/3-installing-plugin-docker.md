# Installing the Plugin in Docker-Based NetBox Deployments

This workflow is not documented as a supported installation path yet.

At the moment:

- the plugin itself is documented for standard NetBox virtual-environment installs
- the separate `proxbox-api` backend can run in Docker

If your NetBox deployment is containerized, you will need to adapt the source-install workflow for your own image build and runtime process.
